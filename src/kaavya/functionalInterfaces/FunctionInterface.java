package kaavya.functionalInterfaces;
import java.util.function.Function;

/*The purpose of Function<T,R> interface is for mapping scenarios where the input is transformed into a different 
 * output type. In Java Streams, the map function of the stream accepts an instance of Function to convert the stream of one type to  
 * be converted into a stream of another type.
 * Function <T,R> can be an assignment target for lambda or method reference.
 * It has one abstract method apply()
 */
public class FunctionInterface {

}
